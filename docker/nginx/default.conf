server {
    listen 8081;
    server_name localhost;

    location / {
        add_header X-Server "nginx-origin" always;
        return 200 "Response from NGINX origin! Path: $request_uri\n";
    }

    location /health {
        return 200 "OK\n";
    }
}